import { Transition } from 'framer-motion'

const transition: Transition = { type: 'tween', duration: 0.3 }
const framerProps = { exit: 'exit', animate: 'enter', initial: 'initial' }

export { transition, framerProps }
